version: "3.9"

services:
  # mysql:
  #   image: mysql:5.7
  #    volumes:
  #      - todo-mysql-data:/var/lib/mysql
  #    environment:
  #      MYSQL_ROOT_PASSWORD: secret
  #      MYSQL_DATABASE: todos
  db:
    # image: postgres
    # environment:
    #   - POSTGRES_DB=postgres
    #   - POSTGRES_USER=postgres
    #   - POSTGRES_PASSWORD=postgres
    image: mysql:5.7
    volumes:
      - hibus-mysql-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: Mysql10093.,
      MYSQL_DATABASE: hibus
  web:
    build: .
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8080"
    volumes:
      - .:/code
    ports:
      - "8080:8080"
    depends_on:
      - db

volumes:
  hibus-mysql-data:
